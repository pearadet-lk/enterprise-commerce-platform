# Docker Compose mode (project: enterprise-commerce-platform)
# Copy to .env in repo root if needed for compose variable substitution.

ECP_DEPLOY_MODE=compose
MSSQL_SA_PASSWORD=Your_strong_Password123
