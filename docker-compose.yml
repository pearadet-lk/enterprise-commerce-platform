# Enterprise Commerce Platform — Docker Compose mode
# Wrapper for deploy/compose/docker-compose.yml
# Project name (root): enterprise-commerce-platform
#
# Usage: docker compose up --build
# See deploy/README.md for all 3 deployment modes.

name: enterprise-commerce-platform

include:
  - path: deploy/compose/docker-compose.yml
